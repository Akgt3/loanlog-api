{
  "name": "loanlog-api",
  "version": "1.0.0",
  "scripts": {
    "start": "node node_modules/.bin/json-server --watch db.json --host 0.0.0.0 --port 10000"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}